
nome, idade, situacao.

Funções built-in usadas:

input(), int(), print().

Estrutura condicional:

if, elif, else.

Estrutura de repetição:

Não é obrigatória nesse projeto, mas posso adicionar se quiser.

Operadores:

<, >=, <=, and.
