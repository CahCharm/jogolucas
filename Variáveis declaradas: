#built-in funções que já vêm prontas para uso dentro de uma linguagem de programação, sem a necessidade de serem definidas pelo usuário 
nome, idade, situacao.

Funções built-in usadas:

input(), int(), print().

Estrutura condicional:

if, elif, else.

Operadores:

<, >=, <=, and.
